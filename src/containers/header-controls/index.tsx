import {
	Notifications,
	//ThemeToggler
} from '@/components';

export const HeaderControls = () => {
  return (
    <div className='ml-auto flex items-center'>
      <Notifications />
    </div>
  );
};
